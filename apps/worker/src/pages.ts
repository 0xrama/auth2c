/**
 * Hosted HTML pages (home, docs, account, error, shell) served by the Worker.
 * The account page's logout handler awaits the now-async Auth2C.signOut().
 */
const esc = (s: string) =>
  s.replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[c] as string);
export const shell = (title: string, body: string) =>
  `<!doctype html><html lang="en"><head><meta charset=utf-8><meta name=viewport content="width=device-width,initial-scale=1"><meta name=theme-color content="#5b55ef"><title>${esc(
    title,
  )}</title><link rel=preconnect href="https://fonts.googleapis.com"><link rel=preconnect href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Manrope:wght@400;500;600;700&display=swap" rel=stylesheet><style>:root{--bg:#f7f7f4;--surface:#fff;--ink:#171719;--muted:#6f6f73;--line:#deded9;--accent:#5b55ef;--danger:#d84444}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--bg);color:var(--ink);font:15px/1.6 Manrope,system-ui,sans-serif}a{color:inherit;text-underline-offset:4px}.wrap{width:min(1120px,calc(100% - 40px));margin:auto;padding-bottom:70px}.nav{height:80px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);font-weight:600}.nav b,.nav>a:first-child{font-size:18px;letter-spacing:-.04em;text-decoration:none}.nav span{display:flex;gap:18px}.nav span a{text-decoration:none;font-size:13px}.hero{padding:120px 0 90px;position:relative}.eyebrow{color:var(--accent);font:500 11px/1 DM Mono,monospace;letter-spacing:.12em}.hero h1,h1{font-size:clamp(54px,9vw,108px);line-height:.94;letter-spacing:-.075em;margin:20px 0 28px;max-width:900px}.hero p{max-width:640px;color:var(--muted);font-size:19px}.hero:after{content:"A2";position:absolute;right:3%;top:95px;width:210px;height:210px;border:1px solid var(--accent);border-radius:50%;display:grid;place-items:center;color:var(--accent);font:500 34px DM Mono;box-shadow:0 0 0 25px var(--bg),0 0 0 26px var(--line)}.card{border:1px solid var(--line);background:var(--surface);border-radius:8px;padding:24px;overflow:hidden}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(245px,1fr));gap:12px}.cta{display:inline-block;background:var(--accent)!important;color:#fff!important;padding:12px 18px;border:0!important;border-radius:6px;text-decoration:none;font-weight:600;cursor:pointer;transition:transform .15s,opacity .15s}.cta:hover{transform:translateY(-1px);opacity:.92}.cta:focus-visible,button:focus-visible,a:focus-visible{outline:3px solid #aaa6ff;outline-offset:3px}h2{font-size:30px;letter-spacing:-.04em;margin-top:64px;margin-bottom:18px}pre{border:1px solid #2e2e34;background:#1c1c20;color:#eeeefa;border-radius:7px;padding:20px;overflow:auto;font:13px/1.7 DM Mono,monospace}code{font-family:DM Mono,monospace;color:var(--accent)}#panel>.card{display:flex!important;background:var(--ink);color:#fff;border:0;padding:22px!important}#panel>.card #email{color:#aaa!important}#panel>.card #logout{background:#fff!important;color:var(--ink)!important;margin-left:auto!important}#signed-out{text-align:center;padding:52px}#signed-out>div{color:var(--danger)!important;font:500 11px DM Mono;letter-spacing:.08em}#sites .card,#sessions .card{min-height:170px;display:flex;flex-direction:column}#sites .card p,#sessions .card p{color:var(--muted)!important;font-size:13px}#sites button,#sessions button{margin-top:auto;align-self:flex-start;background:transparent!important;color:var(--danger)!important;border:1px solid #e7b4b4!important;padding:8px 11px!important;border-radius:5px!important;cursor:pointer}#avatar{object-fit:cover;border:2px solid #fff}body:after{content:"";position:fixed;inset:0;pointer-events:none;opacity:.14;background-image:radial-gradient(#777 0.5px,transparent .5px);background-size:5px 5px}@media(max-width:760px){.wrap{width:min(100% - 28px,1120px)}.nav span a:not(:first-child){display:none}.hero{padding:80px 0 65px}.hero:after{position:relative;right:auto;top:auto;width:110px;height:110px;margin-top:55px;box-shadow:0 0 0 14px var(--bg),0 0 0 15px var(--line)}.grid{grid-template-columns:1fr}#panel>.card #logout{width:100%;margin:12px 0 0!important}#panel>.card{align-items:flex-start!important}}</style></head><body><div class=wrap>${body}</div></body></html>`;
export const home = (base: string) =>
  shell(
    "Auth2C — private identity for every app",
    `<nav class=nav><b>auth2c</b><span><a href=/account>Your account</a><a href=/docs>Documentation</a><a href=/.well-known/openid-configuration>Discovery</a></span></nav><main class=hero><div class=eyebrow>PRIVATE IDENTITY INFRASTRUCTURE</div><h1>One person.<br>Different ID<br>everywhere.</h1><p>Google sign-in without dashboards, client registration, or cross-app tracking. Every origin receives its own private, verifiable identity.</p><p><a class=cta href=/docs>Start building</a> &nbsp; <a href=/account>Manage your identity →</a></p></main><section><div class=eyebrow>THE COMPLETE FLOW</div><h2>Small integration. Serious primitives.</h2><div class=grid><div class=card><b>Request</b><p>Load one hosted script and begin sign-in with mandatory PKCE.</p><pre>&lt;script src="${base}/auth2c.js"&gt;&lt;/script&gt;</pre></div><div class=card><b>Identify</b><p>Google proves the person. Auth2C derives a private identifier for your origin.</p><pre>Auth2C.signIn()</pre></div><div class=card><b>Verify</b><p>Your server validates a short-lived ES256 token using standard JWKS.</p><pre>/.well-known/jwks.json</pre></div></div></section>`,
  );
export const docs = (base: string) =>
  shell(
    "Auth2C docs",
    `<nav class=nav><a href=/>← auth2c</a><b>Documentation</b></nav><h1>Authentication in minutes</h1><p>Auth2C derives the client audience from your callback origin. There is no app registration or dashboard.</p><h2>Browser</h2><pre>&lt;script src="${base}/auth2c.js"&gt;&lt;/script&gt;
&lt;button onclick="Auth2C.signIn({ requestProfile: true })"&gt;Sign in&lt;/button&gt;
&lt;script&gt;console.log(Auth2C.getIdentity())&lt;/script&gt;</pre><h2>React</h2><pre>npm i @auth2c/react
const { identity, loading, signIn, signOut } = useAuth2C()</pre><h2>Flow</h2><div class=grid><div class=card>1. SDK generates state, verifier, and S256 challenge.</div><div class=card>2. Auth2C brokers Google login and profile consent.</div><div class=card>3. Callback exchanges a one-time code for an ES256 token.</div></div><h2>Server verification</h2><pre>const jwks = createRemoteJWKSet(new URL("${base}/.well-known/jwks.json"));
await jwtVerify(token, jwks, {
  issuer: "${base}",
  audience: "origin:https://your-app.com"
});</pre><h2>Endpoints</h2><pre>GET  /authorize                 Start OAuth + PKCE
POST /token                     Exchange one-time code
POST /session/check             Check expiry/revocation
POST /session/revoke            Revoke this session
GET  /.well-known/jwks.json     Signing keys
GET  /.well-known/openid-configuration</pre><p>Tokens last 15 minutes. Never use unverified browser claims for authorization.</p>`,
  );
export const errorPage = (message: string) =>
  shell(
    "Auth2C error",
    `<h1>We couldn't sign you in</h1><p>${esc(message)}</p><p><a href="javascript:history.back()">Go back</a></p>`,
  );
export const account = (base: string) =>
  shell(
    "Your account — Auth2C",
    `<nav class=nav><a href=/>← auth2c</a><b>Account</b></nav><main><section class=hero style="padding-bottom:40px"><div class=eyebrow>IDENTITY CONTROL CENTER</div><h1 style="font-size:clamp(48px,7vw,82px)">Your identity.<br>Your call.</h1><p>Your profile, authenticated apps, and active sessions in one place.</p></section><div id=loading class=card>Checking your authentication…</div><section id=signed-out class=card hidden><div style="color:#ffb8c0;margin-bottom:10px">● NOT AUTHENTICATED</div><h2 style="margin-top:0">Sign in to manage your account</h2><p>Continue with Google to see your Auth2C profile, apps, and active sessions.</p><button class=cta id=login style="border:0;cursor:pointer">Continue with Google →</button></section><section id=panel hidden><div class=card style="display:flex;gap:18px;align-items:center;flex-wrap:wrap"><img id=avatar width=64 height=64 style="border-radius:50%;display:none"><div><div style="color:#8ee7c2;font-size:13px">● AUTHENTICATED</div><b id=name style="font-size:20px">Your account</b><div id=email style="color:#aeb7c9"></div></div><button class=cta id=logout style="margin-left:auto;border:0;background:#ff8f9c;color:#26070c;cursor:pointer">Log out</button></div><h2>Authenticated apps</h2><div id=sites class=grid></div><p id=empty class=card hidden>No third-party apps have been authenticated yet.</p><h2>Active sessions</h2><div id=sessions class=grid></div><p id=no-sessions class=card hidden>No active sessions.</p></section></main><script src="${base}/auth2c.js"></script><script>
const q=s=>document.querySelector(s), api=async(path,options={})=>{const id=Auth2C.getIdentity();const r=await fetch(path,{...options,headers:{'content-type':'application/json',authorization:'Bearer '+(id?.token||''),...(options.headers||{})}});if(!r.ok)throw Error((await r.json()).error||'Request failed');return r.json()};
const button=(label,fn)=>{const b=document.createElement('button');b.textContent=label;b.style.cssText='background:#2b161a;border:1px solid #74333e;color:#ffb8c0;padding:8px 11px;border-radius:7px;cursor:pointer';b.onclick=fn;return b};
async function render(){q('#loading').hidden=false;const id=Auth2C.getIdentity();q('#loading').hidden=true;q('#signed-out').hidden=!!id;q('#panel').hidden=!id;if(!id)return;q('#name').textContent=id.name||'Your account';q('#email').textContent=id.email||'Authenticated with a private identity';if(id.picture){q('#avatar').src=id.picture;q('#avatar').style.display='block'}try{const data=await api('/account/overview');q('#name').textContent=data.account.name||id.name||'Your account';q('#email').textContent=data.account.email||id.email||'Private identity';const pic=data.account.picture||id.picture;if(pic){q('#avatar').src=pic;q('#avatar').style.display='block'}const apps=q('#sites');apps.replaceChildren();q('#empty').hidden=!!data.apps.length;for(const g of data.apps){const card=document.createElement('article');card.className='card';const title=document.createElement('b');title.textContent=g.origin===location.origin?'Auth2C Account':g.origin;const meta=document.createElement('p');meta.style.color='#aeb7c9';meta.textContent=g.profileAllowed?'Name, email & photo shared':'Private identity only';card.append(title,meta,button('Revoke app',async()=>{if(confirm('Revoke '+g.origin+'?')){await api('/account/grants/revoke',{method:'POST',body:JSON.stringify({origin:g.origin})});render()}}));apps.append(card)}const sessions=q('#sessions');sessions.replaceChildren();q('#no-sessions').hidden=!!data.sessions.length;for(const s of data.sessions){const card=document.createElement('article');card.className='card';const title=document.createElement('b');title.textContent=s.id===data.currentSessionId?'This session':s.origin;const meta=document.createElement('p');meta.style.color='#aeb7c9';meta.textContent='Signed in '+new Date(s.created_at).toLocaleString()+' · expires '+new Date(s.expires_at).toLocaleTimeString();card.append(title,meta,button(s.id===data.currentSessionId?'Log out this session':'Revoke session',async()=>{await api('/account/sessions/revoke',{method:'POST',body:JSON.stringify({sessionId:s.id})});if(s.id===data.currentSessionId){await Auth2C.signOut()}render()}));sessions.append(card)}}catch(e){q('#empty').hidden=false;q('#empty').textContent='Your profile is authenticated, but account activity could not load: '+e.message}}
q('#login').onclick=()=>Auth2C.signIn({requestProfile:true,redirectUri:location.origin+'/account',returnTo:'/account'});q('#logout').onclick=async()=>{const id=Auth2C.getIdentity();if(id)try{let part=id.token.split('.')[1].replace(/-/g,'+').replace(/_/g,'/');while(part.length%4)part+='=';const payload=JSON.parse(atob(part));await api('/account/sessions/revoke',{method:'POST',body:JSON.stringify({sessionId:payload.jti})})}catch(_){}await Auth2C.signOut();render()};addEventListener('auth2c:change',render);Auth2C.handleCallback().then(render).catch(e=>{q('#loading').textContent='Sign-in could not finish: '+e.message;q('#loading').hidden=false});
</script>`,
  );
